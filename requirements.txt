# Core runtime dependencies for storefront-cohort.
# The tool runs the full report pipeline with just these two.
pandas>=1.5
numpy>=1.23

# --- Optional ---
# Probabilistic CLV (BG/NBD + Gamma-Gamma). Without it, CLV falls back to a
# built-in heuristic. Install with: pip install lifetimes
#   lifetimes>=0.11
#
# Advanced ML modules under storefront_cohort/advanced/ (not on the core path):
#   scikit-learn>=1.1
#   shap>=0.41
#
# Running the tests:
#   pytest>=7.0
